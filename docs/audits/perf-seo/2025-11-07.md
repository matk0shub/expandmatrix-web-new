# Performance & SEO audit — 2025-11-07

## Scores

| Profile | Performance | Accessibility | Best Practices | SEO |
|---------|-------------|---------------|----------------|-----|
| Mobile  | 0.88        | 1.00          | 1.00           | 1.00 |
| Tablet  | 0.73        | 1.00          | 1.00           | 1.00 |
| Desktop | 0.99        | 1.00          | 1.00           | 1.00 |

_Production build served via `pnpm start`; Lighthouse 13.0.1 (mobile/tablet/desktop). Reports stored in `docs/lighthouse/reports`._

## Improvements observed
- Best Practices now 1.00 thanks to lazy Cal.com and nav a11y fixes; third-party cookies no longer penalise score.
- Mobile Performance jumped from 0.74 → 0.88; throttled LCP dropped from 13.6 s to 3.8 s.
- Redirect chain persists in the simulated environment (Lighthouse still sees `/ → /en`). Accept-Language optimisation is in middleware but throttled test hits 127.0.0.1 and still observes the redirect; in production expect savings.

## Remaining watch points
- **LCP (mobile)**: at 3.8 s simulated (score 0.55). Real-world LCP is around 1 s. Opportunities:
  - Continue trimming the 6544 chunk; 33 KB of unused JS remains.
  - Investigate hero imagery/background effects to ensure lazy loading.
- **Tablet performance (0.73)**: similar LCP considerations; consider additional code splitting for tablet experience.
- **Redirect audit**: confirm in production once deployed; if Lighthouse still reports, consider running with `--locale=en` or update middleware to short-circuit more aggressively.

## Next optimisation targets
1. Finish bundle diet by dynamic importing animation-heavy sections (hero/process) once they enter viewport.
2. Validate caching headers on production/CDN (make sure `_next/image` and font assets honour immutable caching).
3. Set up automated Lighthouse run (GitHub Action) to track scores.

## Artifacts
- Lighthouse JSON/HTML: `docs/lighthouse/reports/{mobile,tablet,desktop}.{json,html}`
- Audit note: this file
- Commands run:
  ```bash
  pnpm build
  pnpm start
  pnpm audit:mobile
  pnpm audit:tablet
  pnpm audit:desktop
  ```

---
## Follow-up run (later 2025-11-07)

| Profile | Performance | Accessibility | Best Practices | SEO |
|---------|-------------|---------------|----------------|-----|
| Mobile  | 0.90        | 1.00          | 1.00           | 1.00 |
| Tablet  | 0.73        | 1.00          | 1.00           | 1.00 |
| Desktop | 1.00        | 1.00          | 1.00           | 1.00 |

Highlights:
- Hero tweaks (instant framer + preloaded logo) improved mobile perf to 0.90; simulated LCP now 3.6 s.
- Tablet still trails at 0.73; future work: dynamic import process/hero sections per viewport.
- Redirect audit still shows `/ → /en` under localhost; expect improvement on production once middleware deploys.
- Unused JS audit continues to flag chunk `6544…` (~33 KB). Next steps: split hero/process effects further.
